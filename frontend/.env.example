# Public API path only. Never put Vault tokens or database passwords in VITE_ variables.
VITE_API_URL=/api/v1
# Vite development proxy; backend APP_ORIGIN must equal the browser origin.
API_PROXY_TARGET=http://127.0.0.1:3100
