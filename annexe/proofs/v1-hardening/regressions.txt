{
  "status": "PASS",
  "checks": [
    "disabled account excluded from candidates, notifications, selection and assignment; CLI removes multiple sessions",
    "reactivation, session revocation and confirmed assignment preservation",
    "saturated upload quota still produces readable mission confirmation PDF",
    "two real SQL connections: cleanup skips uncommitted file and removes actual orphan",
    "bank replacement at quota limit counts only active version, retained previous version remains superseded"
  ]
}
